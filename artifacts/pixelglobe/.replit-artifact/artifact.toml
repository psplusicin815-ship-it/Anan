kind = "web"
previewPath = "/"
title = "PixelGlobe"
version = "1.0.0"
id = "artifacts/pixelglobe"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20154

[services.development]
run = "pnpm --filter @workspace/pixelglobe run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pixelglobe", "run", "build" ]
publicDir = "artifacts/pixelglobe/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20154"
BASE_PATH = "/"
